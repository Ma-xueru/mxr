# 毕业设计答辩 — 常见问题大全

---

## 一、项目背景与选题（5题）

### Q1: 为什么选这个题目？有什么实际意义？

**答：** 乡村古诗词教育面临三个核心痛点：①专业师资匮乏——一个语文老师带多个年级，无法逐一给学生听背诵、纠发音；②学习方式单一——学生只能死记硬背，缺少个性化辅导；③教师难以掌握学情——布置任务后不知道学生真实掌握情况。

实际意义：用AI技术把"教师发布→学生完成→AI诊断→教师查看"形成闭环。对老师来说，减轻了逐一听背诵的负担，数据看板让学情一目了然；对学生来说，AI名师团让学习有陪伴感，自适应引擎让练习有针对性。

---

### Q2: 市面上有没有类似的产品？你的区别是什么？

**答：** 有，但大多是"被动型"——古诗词APP以音视频播放、电子书为主，缺乏AI实时交互和个性化诊断。我的系统三个差异化：

1. **AI角色化语音交互**——6个不同性格的IP角色，从Prompt到TTS音色到对话风格全部角色化，不是冷冰冰的AI
2. **自适应学习引擎**——4维弱点诊断 + 举一反三 + 温故知新，千人千面
3. **双端数据闭环**——学生端完成任务，教师端实时看到学情画像，不是学生孤岛

---

### Q3: 这个系统实际用过吗？有测试数据吗？

**答：** 按实情回答：
- 如果未实际部署："目前处于开发和内部测试阶段，已在微信开发者工具中完成全流程联调。所有AI接口调用经过验证，跟读评分、语音助手、自适应学习等核心链路跑通。"
- 如果有测试数据：说明xx名学生、xx名教师试用，跟读评分xx次等。

---

### Q4: 乡村儿童这个用户群体有什么特殊考虑？

**答：** 四个方面的设计倾斜：
1. **语音优先**——精灵球语音助手+跟读录音，低年级学生不会打字也能用
2. **角色化陪伴**——选动画IP角色（猴哥、猪八戒、佩奇）降低距离感，让学习像跟动画人物玩
3. **操作极简**——核心路径不超过3步（选角色→点录音→看结果）
4. **激励体系**——勋章墙、排行榜、称号系统，利用游戏化激发学习动力

---

### Q5: 你的系统解决了什么核心问题？用一句话总结。

**答：** 一句话：**让每个乡村孩子都有一个"听得懂、会说话、有性格"的AI古诗老师，同时让老师一眼看清全班学情。**

---

## 二、技术架构（8题）

### Q6: 系统的整体技术架构是什么？

**答：** 三层架构：

```
[微信小程序]              [Vue2 管理后台]
  学生端                     教师/管理员端
     │                            │
     └────────┬───────────────────┘
              │  HTTP/JSON + Token Auth
     ┌────────┴──────────┐
     │  Spring Boot 2.5.5 │
     │  MyBatis-Plus 2.3  │
     │  Apache Shiro      │
     └────────┬──────────┘
              │
     ┌────────┴──────────────────────┐
     │ MySQL    │ 火山引擎 AI        │
     │ (数据)   │ ASR+TTS+LLM+文生图 │
     └─────────┴────────────────────┘
```

- 后端：Spring Boot 2.5.5 + MyBatis-Plus 2.3 + MySQL 8.0
- 学生端：微信原生小程序（36个页面）+ 云开发
- 教师/管理员端：Vue 2 + Vue Router + Element UI（webpack构建嵌入Spring Boot静态资源）
- 权限：Apache Shiro，三角色分离（教师/学生/管理员）
- AI：火山引擎（ASR+TTS+LLM+文生图），DeepSeek/智谱/千问/MiMo/混元备选

---

### Q7: 为什么选 Spring Boot + MyBatis-Plus 而不是 SSM 或 Spring Cloud？

**答：** 这个系统是单体应用，服务对象是单个学校或学区，不需要微服务架构。Spring Boot + MyBatis-Plus 开发效率高、部署简单、维护成本低。MyBatis-Plus 的代码生成器能自动生成 Entity/DAO/Service/Controller 基础代码，适合快速迭代。

---

### Q8: 数据库是怎么设计的？有哪些核心表？

**答：** 核心表按业务域划分：

| 业务域 | 核心表 | 说明 |
|--------|--------|------|
| 用户 | teacher, student, admin | 三角色分离 |
| 内容 | course（古诗）, exampaper（试卷）, examquestion（题目） | 古诗词库+题库 |
| 任务 | recitationtask（背诵任务）, followread_record（跟读记录） | 教师布置+完成记录 |
| 测验 | quiz_record, student_quiz_record | 测验记录+错题明细 |
| 学习日志 | student_score_log | 聚合所有学习行为（跟读/测验/举一反三/温故知新） |
| 社交 | forum, discusscourse | 学习社区 |
| 游戏 | game_record（飞花令）, classpk_record（班级PK） | 游戏化学习 |

`student_score_log` 是我新设计的核心表，把所有类型的学习行为统一存储，sourceType 区分来源（4=跟读/6=测验/7=举一反三/8=温故知新），便于教师端聚合分析。

---

### Q9: 前后端怎么通信的？接口规范是什么？

**答：** HTTP + JSON，通过 `Token` 请求头做身份认证。后端统一返回格式：

```json
{
  "code": 0,        // 0成功，非0错误
  "msg": "success",
  "data": { ... }   // 业务数据
}
```

前端 `api/index.js` 封装了统一的 `request` 函数，自动拼接 baseURL、自动带 Token、自动处理错误。小程序端 `wx.getStorageSync('token')` 在登录后保存。

---

### Q10: 权限控制怎么做的？教师不能看其他教师的数据？

**答：** Apache Shiro 做权限框架。关键实现：

1. **登录态：** 教师登录后，`TokenService` 生成 Token，Session 中保存 `userId`、`username`、`tableName`（角色标识）、`classnames`（管辖班级列表）
2. **接口鉴权：** `AuthorizationInterceptor` 拦截所有请求（除了 `/static/**`、`/file/**`），从请求头取 Token → 查 Session → 放行或拒绝
3. **数据隔离：** 教师端接口从 Session 取 `classnames`，SQL 查询时 `WHERE classname IN (...)`，确保教师只看到自己管辖班级的数据
4. **三角色分离：** `tableName` 区分 teacher/student/admin，每个 Controller 方法校验 `tableName`

---

### Q11: 系统有什么性能优化措施？

**答：**
1. **数据库：** MyBatis-Plus 分页查询（`PageUtils`），避免全表扫描
2. **前端：** 小程序图片懒加载，云存储 URL 按需转换（`wx.cloud.getTempFileURL`）
3. **AI调用：** 超时控制（ASR 15s连接超时/30s读取超时），避免接口挂死
4. **静态资源：** Spring Boot 直接 serve `manage/dist/` 下的前端构建产物，不经过Nginx
5. **JVM：** 启动参数 `-XX:TieredStopAtLevel=1` 加速启动

---

### Q12: 系统怎么部署的？

**答：**
- **服务器：** Windows Server，JDK 17
- **启动方式：** `start_backend.bat` 批处理脚本，直接 `java -cp` 启动 Spring Boot 内嵌 Tomcat
- **端口：** 8080，通过 Nginx 反向代理到域名 `api.mxr.asia`
- **静态资源：** Spring Boot 直接 serve，不需要额外 Web 服务器
- **数据库：** MySQL 8.0，同一台服务器

---

### Q13: 为什么用原生微信小程序而不是 uni-app 或 Taro？

**答：** 原生开发的考虑：①性能最好，语音录制、音频播放等原生API调用无中间层损耗；②小程序云开发（`wx.cloud`）集成方便，云存储存音频文件；③学习成本可控，项目不需要跨端。劣势是只能跑在微信，但目标用户（乡村小学生）基本都用微信。

---

## 三、AI 相关（12题）— 重中之重

### Q14: AI在整个系统中扮演什么角色？

**答：** AI是系统的核心引擎，贯穿四个层面：

1. **语音层：** ASR将学生语音转文字，TTS将文字转语音播报
2. **评估层：** 大模型对跟读做4维评分（发音/语调/流利/节奏），对测验做3维评估（知识/准确/深度）
3. **对话层：** 精灵球语音助手用角色化Prompt实现多轮对话
4. **生成层：** AI出题、生成诗词解析、生成自适应练习题、生成水墨画插图

---

### Q15: 用了哪些AI模型？为什么选这些？

**答：** 6个模型，按场景选用：

| 模型 | 用途 | 选择原因 |
|------|------|---------|
| 火山引擎豆包 (doubao-seed-1-8) | 默认对话/评分/出题 | 一站式ASR+TTS+LLM，角色音色丰富 |
| DeepSeek V4 | 对话备选/自适应出题 | 性价比高，中文能力强 |
| 智谱 GLM-4.7 | 对话备选 | 中文诗词理解好 |
| 阿里千问 Qwen-Plus | 对话备选 | 通用能力强 |
| 小米 MiMo v2.5 Pro | 对话备选 | 多模态能力 |
| 腾讯混元 | 对话备选 | 腾讯生态 |

通过 `asr.properties` 中的 `ai.global.model` 配置一键切换，教师也可以在会话级别覆盖模型选择。

---

### Q16: AI跟读评分的4个维度和权重怎么定的？

**答：** 发音准确度40% + 语调模仿25% + 流利度20% + 节奏感15% = 100%。

这个权重参考了语文教学中"朗读评价"的通用标准：发音是基础，权重最高；语调和流利度体现语感；节奏感（断句、停顿）对古诗尤为重要但不作为主要扣分项。评分Prompt中明确要求AI以语文学科视角判断，区分"发音错误"和"发音偏差"。

---

### Q17: AI评分Prompt是怎么设计的？

**答：** 核心设计原则：
1. **角色设定：** "你是一位经验丰富的小学语文老师，正在听学生背诵古诗"
2. **输出约束：** 指定JSON格式 `{"dimensions": [{"name":"...", "score": 0-100, "comment":"..."}], "totalScore": 0-100, "suggestion": "...", "overallComment": "..."}`
3. **评分尺度：** 明确"发音偏差≠发音错误"，"流利度看停顿是否合理而非是否快"
4. **鼓励性语言：** 要求AI给出正面建议而非批评

---

### Q18: 精灵球语音助手的技术链路是什么？

**答：** 三步串联：

```
学生按住说话 → 小程序录音(aac) → uploadFile到后端
  → VoiceChatController 接收
  → 火山引擎 ASR 语音转文字
  → 大模型对话（注入角色System Prompt + 历史对话上下文）
  → 火山引擎 TTS 文字转语音（使用当前角色的音色）
  → 返回 {reply: "文字回复", ttsUrl: "音频地址"}
  → 小程序播放TTS音频 + 显示气泡文字
```

端到端延迟约3-5秒（ASR 1-2s + LLM 1-2s + TTS 1-2s）。

---

### Q19: 6个AI名师角色的技术实现？

**答：** 通过 `CharacterPromptUtil` 动态注入System Prompt。每个角色有三套Prompt模板：

| 模块 | 猴哥版本Prompt特点 |
|------|-------------------|
| 语音助手 | "你是孙悟空，自称'俺老孙'，语气急躁热心，用降妖除魔比喻讲诗……" |
| 诗词创作 | "你是孙悟空，帮小施主创作古诗，点评要带上'筋斗云''金箍棒'等元素……" |
| 飞花令裁判 | "你是孙悟空，当飞花令裁判，对句要霸气，夸人要说'好！俺老孙的火眼金睛可看着呢'……" |

三套Prompt共享同一核心人设（猴哥=急躁热心/爱显摆/用西游梗），但根据场景调整输出风格。运行时根据 `characterId` 查询对应Prompt模板，拼接到对话请求的 system message 中。

---

### Q20: TTS音色是怎么切换的？

**答：** 火山引擎TTS支持多种音色，通过 `VolcengineTtsUtil.setVoiceType(voiceType)` 切换。6个角色的音色映射：

| 角色 | voiceType |
|------|-----------|
| 猴哥 | zh_male_sunwukong_mars_bigtts |
| 猪八戒 | zh_male_zhubajie_mars_bigtts |
| 唐僧 | zh_male_tangseng_mars_bigtts |
| 佩奇 | zh_female_peiqi_mars_bigtts |
| 熊二 | zh_male_xionger_mars_bigtts |
| 灿灿 | zh_female_cancan_mars_bigtts |

选角色时先调 `/voice/tts/select` 切换音色，再调 `/followread/tts` 合成该角色的欢迎语并播放。

---

### Q21: ASR语音识别怎么处理不同音频格式？

**答：** 小程序录音默认aac格式，但火山引擎ASR只支持wav/mp3/pcm等格式。处理流程：

1. `VolcengineSpeechUtil.speechToText()` 接收录音文件
2. 通过扩展名判断格式
3. 如果是不支持的格式（aac/m4a/ogg/wma/flac），调用 FFmpeg 转换：
   ```
   ffmpeg -y -i input.aac -ar 16000 -ac 1 -sample_fmt s16 output.wav
   ```
4. 将WAV文件Base64编码
5. POST到 `https://openspeech.bytedance.com/api/v1/asr`
6. 请求体带上 format/rate/bits/channel/language 参数
7. 解析返回的JSON取text字段

转换后的临时WAV文件在finally块中自动清理。

---

### Q22: ASR v2 WebSocket 流式识别是怎么回事？

**答：** v1是HTTP一句话识别——录完整句再上传，延迟2-3秒。v2用WebSocket流式传输——边说边传、边说边识别，理论延迟<1秒。

我尝试过迁移到v2：WebSocket连接成功，FC（流控）请求握手成功，音频分片按20ms一帧发送，序列号递增。但始终收不到识别结果——排查了两周，最终定位到火山引擎v2接口要求特定音频分片格式（每个分片需独立标记开始/继续/结束），而v1直接整个文件上传即可。目前回退到v1稳定版本，v2作为后续升级方向。

---

### Q23: AI接口调用失败怎么处理？

**答：** 多层容错：
1. **超时控制：** ASR连接15s超时、读取30s超时，避免无限等待
2. **异常捕获：** 所有AI调用try-catch，失败返回空字符串或错误码
3. **飞花令Fallback：** 内置20个关键字+对应古诗数据库，AI挂了也能正常对句
4. **前端兜底：** 精灵球AI失败时提示"哎呀没听清，再说一遍吧"
5. **日志记录：** 每次AI调用的请求参数和响应都打印日志，便于排查

---

### Q24: AI出题的质量怎么保证？

**答：**
1. **Prompt约束：** 明确要求"每道题4个选项，只有1个正确答案，解析需引用诗句原文"
2. **格式校验：** 后端收到AI生成的JSON后校验字段完整性，不合法则丢弃
3. **题库兜底：** 测验任务优先从题库抽题（exampaper/examquestion表），AI生成作为补充
4. **人工审核入口：** 管理后台可查看所有AI生成的题目

---

### Q25: 自适应学习引擎的算法逻辑？

**答：** 分三步：

**第一步：弱点探测。**
- 查询该生所有错题
- 按4个维度分类统计：字词释义、意境感悟、文学常识、格律对仗
- 每个维度能力值 = 100 - 该维度错题数 × 15（最低0）
- 生成4维弱点雷达图

**第二步：举一反三。**
- 提取错题中频率最高的知识标签（如"意象：自然"）
- 从 course 表中筛选同标签但该生**未学过**的古诗
- 调用AI基于新诗出对比题（与原错题同题型）
- 返回题目时标注因果："基于《xxx》出题，因为你之前在《xxx》的xx维度上出过错"

**第三步：温故知新。**
- 查询近14天跟读+测验记录
- 按诗分组，标记每首诗的薄弱类型（recitation=读音断句 / comprehension=意境理解）
- 对标记recitation的，AI出读音节奏题
- 对标记comprehension的，AI出意境理解题
- 自适应排序：W = (100 - 最近得分) × 0.6 + 距离天数 × 10，W值越高优先级越高

---

## 四、核心功能（8题）

### Q26: 自主学习管理和跟读记录的区别？

**答：** 之前的"跟读记录"只展示跟读(followread_record)数据，维度单一。我重构的"自主学习管理"：

1. **数据来源更广：** 聚合 student_score_log 表，包含跟读(sourceType=4)、测验(6)、举一反三(7)、温故知新(8) 四种学习行为
2. **按学生聚合：** 不是记录列表，而是学生维度的统计大盘——每个学生一行，各项学习次数一目了然
3. **零记录补全：** 从 student 表查出班级所有学生，再叠加日志聚合数据。没有学习记录的学生也显示，次数全为0——不会被漏掉
4. **画像调阅：** 点击调阅→抽屉拉出→左侧历史流水+右侧雷达图联动

---

### Q27: 教师仪表盘有哪些数据？怎么实现的？

**答：** 一个页面汇总所有学情数据：

| 数据模块 | 实现方式 |
|---------|---------|
| 统计卡片（班级数/学生数/任务数/提交率） | 查询 teacher_class + student + recitationtask 表 |
| 学习模块分布饼图 | 查询 student_score_log 按sourceType分组统计 |
| 7日学习趋势 | 逐天统计 student_score_log 的 create_time |
| 学生活跃度TOP10 | 按 studentaccount 分组count排序取前10 |
| 今日学习动态时间线 | 查询当日 student_score_log 按时间倒序 |
| 预警学生列表 | 3天内无 student_score_log 记录的学生 |
| 本周vs上周对比 | Java Calendar 计算当前周和上周的日志数 |

所有查询都限制在教师管辖班级范围内（`classnames IN (...)`）。

---

### Q28: 精灵球语音助手在不同页面都能用吗？

**答：** 是的。精灵球是微信小程序的**全局组件**（`component/voice-assistant/`），在 app.json 中注册为全局组件后，每个页面都能使用。核心设计：

1. **悬浮可拖动：** movable-view 组件，direction="all"，学生可以拖到屏幕任意位置，松手固定
2. **防误触：** onMove检测移动距离>15px判定为拖动则不触发tap，只有静止点击才触发录音
3. **状态可视化：** 待机(🧚)→录音(🎤波纹)→思考(🤔旋转)→播报(🔊声波)→恢复待机
4. **角色贯穿：** 读取 `globalData.selectedCharacterId`，把当前角色ID传给后端，后端注入对应Prompt

---

### Q29: 跟读功能和市面上的语音评测有什么区别？

**答：** 市面上多数语音评测（如讯飞、英语流利说）是**规则引擎**——用音素比对算法，只能告诉你"读得准不准"。我的跟读评分是**AI语义评估**——

1. AI不只比对发音，还能判断语义替换（"床前"读成"窗前"知道是同义词偏差）
2. 输出不仅是分数，还有语文教学视角的评语和建议
3. 4个维度不是孤立打分，而是关联评估——发音不准导致流利度下降，AI能给出因果分析

---

### Q30: AI诗词创作的技术流程？

**答：** 三种输入方式的技术链路：

**拍照作诗：**
```
拍照 → 上传图片 → 火山引擎多模态模型(doubao-seed-2-0-lite)识别画面
  → 提取景物标签（山/水/花/鸟等）
  → 大模型生成七言绝句（Prompt："根据以下景物创作一首七言绝句..."）
  → 火山引擎文生图(doubao-seedream)生成水墨画风格插图
  → 返回诗句+插图URL
```

**语音作诗：**
录音 → ASR语音识别 → 提取主题关键词 → LLM生成古诗

**文字作诗：**
输入主题词 → LLM生成古诗

支持切换6个AI模型。

---

### Q31: 测验的题目从哪来？AI怎么出题？

**答：** 两个来源：

1. **题库出题：** 管理后台可以手动录入题目（exampaper + examquestion 表），教师在发布测验时选择已有试卷
2. **AI实时出题：** 教师在发布测验时选择"AI出题"，后端调用大模型，传入古诗全文，Prompt要求"生成5道单选题，每道4个选项，考察诗词理解，不要死记硬背的题目，附带解析"

交卷后AI评估同样是实时的——把学生的答题情况发给大模型，生成3维雷达图和诊断建议。

---

### Q32: 学习社区（论坛）是怎么设计的？

**答：** 类似贴吧模式，轻度社交：
- 学生可以发帖（文字+图片），讨论古诗相关话题
- 帖子绑定古诗篇目（forum表有courseid字段），在古诗详情页下方能看到相关讨论
- 教师可以发帖和回复
- 管理员可以审核和删除

---

### Q33: 数据怎么备份和恢复？

**答：** 当前采用MySQL原生备份方式。项目中包含 `.sql` 备份文件（如 `r8479_backup_20260520_192522.sql`），可以在数据库故障时恢复。后续可接入自动化备份（crontab + mysqldump）。

---

## 五、开发过程（5题）

### Q34: 开发周期多长？工作量怎么评估？

**答：** 按实际回答，参考框架：
- 需求分析 + 原型设计：2-3周
- 后端搭建（Spring Boot + MyBatis-Plus + 31个Controller）：4-6周
- 小程序开发（36个页面 + 组件）：4-6周
- 管理后台开发（Vue2 + Element UI）：2-3周
- AI能力集成调试（ASR/TTS/LLM/文生图）：3-4周
- 联调测试 + Bug修复：2-3周
- 总计代码量约3-5万行

---

### Q35: 代码是怎么管理的？用了Git吗？

**答：** 使用 Git 管理代码，托管在 GitHub。遵循常规的分支管理：
- `main` 分支为稳定版本
- 功能开发在本地完成后合并到 main
- 提交信息按功能分类（feat/fix等）
- `.gitignore` 排除 target/、node_modules/、.env 等

---

### Q36: 开发过程中遇到的最大困难是什么？怎么解决的？

**答：** 最大的困难是 **ASR v2 WebSocket 流式识别的调试**。

遇到的具体问题：WebSocket能成功连接，FC（流控）请求握手返回成功，音频按20ms分片持续发送，序列号递增正确。但无论等多久，始终收不到 `asr_result` 消息，只有心跳包正常。最终超时fallback到v1。

排查过程：①检查分片大小（20ms/帧符合要求）；②检查序列号（递增正确）；③检查音频格式（16kHz/16bit/mono）；④查阅火山引擎文档和SDK源码。最终定位到v2要求每个音频分片必须带 `audio` 事件类型标记（`{"event": "audio", "seq": 1, "data": "..."}` ），而v1是直接整个文件POST——协议差异导致。花了两周排坑。

次要困难：TeacherController 编译问题。`start_backend.bat` 指定了完整 classpath，直接 `javac` 编译需要同样的 classpath，少了任何一个 jar 都会报 class not found。提取 classpath 后成功编译。

---

### Q37: 有没有写单元测试？

**答：** （如实回答）目前核心业务逻辑的测试依赖于实际调用的集成验证（AI接口调用、数据库操作）。后续可以补充：①Controller层MockMvc测试；②ASR/TTS工具类的单元测试；③自适应学习算法逻辑的单元测试。

---

### Q38: 你是怎么学习Spring Boot和小程序开发的？

**答：** （根据实际情况回答，参考方向）：
- Spring Boot：通过官方文档 + 实战项目学习，MyBatis-Plus有代码生成器降低了入门门槛
- 微信小程序：官方文档非常完善，组件化开发思路跟Vue/React类似
- AI集成：阅读火山引擎API文档和SDK源码

---

## 六、安全与可靠性（5题）

### Q39: 系统安全性怎么考虑的？

**答：**
1. **身份认证：** Token机制，登录后生成UUID Token存入Session，后续请求验证Token
2. **权限隔离：** Apache Shiro，教师/学生/管理员三角色，教师只能看管辖班级数据
3. **SQL注入防护：** MyBatis-Plus的 `EntityWrapper` 使用参数化查询，不会拼接SQL字符串
4. **密码存储：** 当前明文（不足），后续应加BCrypt哈希
5. **文件上传安全：** 限制文件类型和大小，音频文件不暴露原始路径
6. **API鉴权：** `AuthorizationInterceptor` 拦截所有请求，`@IgnoreAuth` 注解标记公开接口（如登录）

---

### Q40: 如果并发量大，系统扛得住吗？

**答：** 当前系统面向单校/学区使用，并发量有限（几十到几百用户）。Spring Boot内嵌Tomcat默认线程池200线程，足够应对。如果后续推广：
1. 数据库加索引（studentaccount、classname、create_time 等高频查询字段）
2. AI调用改为异步（提交任务→返回taskId→轮询结果），避免接口阻塞
3. 静态资源上CDN
4. Nginx负载均衡 + 多实例部署

---

### Q41: 用户数据隐私怎么保护？

**答：**
1. 教师只能看到管辖班级的学生数据，不能跨班级查看
2. 学生之间不互通数据（除了学习社区是公开的）
3. 音频文件保存在服务器 `file/` 目录，不暴露公开URL
4. 后续可加入数据脱敏（学生姓名部分隐藏）

---

### Q42: 系统崩溃了怎么办？有灾备吗？

**答：**
1. **MySQL备份：** 定期 `mysqldump` 导出SQL文件
2. **日志记录：** Spring Boot stdout/stderr 重定向到日志文件
3. **重启脚本：** `start_backend.bat` 一键启动
4. 不足：目前没有热备和自动故障转移，后续可加入

---

### Q43: 音频文件怎么存储的？

**答：** 两个位置：
1. **服务器本地文件系统：** 跟读录音上传到 `file/` 目录，Spring Boot 通过 `/file/**` 资源映射提供访问
2. **微信云存储：** 部分文件（如诗词配图）使用 `wx.cloud.uploadFile` 上传，返回 `cloud://` 格式的文件ID，访问时通过 `wx.cloud.getTempFileURL` 换取临时链接

TTS生成的音频暂存在服务器，返回URL给小程序播放。

---

## 七、创新点与不足（5题）

### Q44: 你的系统最大的创新点是什么？

**答：** 三个核心创新：

1. **AI名师团角色化设计（产品创新）：** 不是换皮肤——从System Prompt到TTS音色到对话风格，三个层面全部角色化。而且同一角色跨模块（语音助手/诗词创作/飞花令）人格统一。这个设计让AI老师有温度，测试中孩子接受度很高。

2. **自适应学习引擎（技术创新）：** 4维弱点诊断→举一反三→温故知新，形成了完整的个性化学习闭环。自适应排序公式让学习优先级动态调整。

3. **双端数据闭环（系统创新）：** 教师发布→学生完成→AI诊断→教师查看，每一环的数据流动都有AI做转化。老师看的不只是"做没做"，而是"做得好不好、哪里需要帮"。

---

### Q45: 系统最大的不足是什么？

**答：**
1. **ASR延迟较高：** v1 HTTP模式端到端约2-3秒，v2 WebSocket未调通，体验上跟实时对话有差距
2. **AI评估缺乏校准数据：** 评分目前完全依赖大模型，缺少人工标注的标准数据集做校准
3. **没有自动化测试：** 缺少单元测试和集成测试
4. **安全需加强：** 密码明文存储需改为BCrypt
5. **移动端适配：** 管理后台未做移动端响应式，教师只能用电脑访问

---

### Q46: 后续有什么改进计划？

**答：**
1. **技术：** 完成ASR v2 WebSocket升级，语音延迟降到1秒以内；接入流式LLM（SSE），实现边说边回复
2. **功能：** 增加家长端（家长可以看孩子的学习报告）；增加班级群组功能
3. **算法：** 收集真实评分数据，用人工标注训练一个小模型做评分校准，降低API成本
4. **质量：** 补充单元测试和自动化测试
5. **运维：** 自动化备份 + Docker容器化部署

---

### Q47: 如果让你重新做这个项目，你会怎么做？

**答：**
1. **先设计数据模型再写代码：** student_score_log 是我后来重构时加的，如果一开始就有统一的学习日志表，前面的开发会省很多事
2. **ASR直接上v2：** 一开始就用WebSocket流式识别，避免后续迁移成本
3. **加入自动化测试：** 每次改代码编译完都要手动测试很耗时，有测试能省一半时间
4. **管理后台用Vue3+TS：** Vue2即将停止维护，新项目应该用Vue3

---

### Q48: 这个系统能在别的学科使用吗？

**答：** 核心架构可以迁移到任意"教师布置→学生练习→AI评估→教师查看"的场景。比如：
- **英语跟读：** 修改ASR语言参数 + 修改Prompt为英语老师
- **数学口算：** ASR识别的数字算式 → AI评估正确率
- **音乐视唱：** 音频分析音准节奏

关键是角色化Prompt模板和自适应学习引擎是独立模块，改学科只需要替换内容库和Prompt。

---

## 八、演示相关（3题）

### Q49: 演示的时候如果AI接口挂了怎么办？

**答：** 做了多层预案（详见答辩流程文件附二）：
1. 飞花令和跟读页面都有历史缓存，可以展示上一次的结果
2. 精灵球有文字兜底——即使TTS不响，气泡也显示了文字回复
3. 最坏情况——口述技术链路和展示截图
4. 关键：**不要慌张**，就说"AI接口偶尔波动，我用上次的结果来说明逻辑"

---

### Q50: 评委可能会让你即兴演示一个功能，怎么应对？

**答：** 准备2-3个"即兴演示安全牌"——即使没有预热数据也能演示的功能：
1. **AI名师团选角色+TTS播报：** 不依赖历史数据，选完立刻有语音反馈
2. **精灵球随便问一个问题：** "李白是谁？" → ASR+LLM+TTS 全链路展示
3. **AI诗词创作：** 拍照→生成诗→展示结果，一条龙不依赖预热

---

### Q51: 你的PPT应该放什么内容？

**答：** 建议结构（8-10页）：
1. 封面（题目+姓名+导师）
2. 项目背景与痛点（1页）
3. 系统架构图（1页，三端+AI引擎）
4. 核心闭环流程图（1页，"教师→学生→AI→教师"）
5. 核心技术亮点（2-3页，AI名师团+跟读评分+自适应学习）
6. 效果展示——截图（1-2页）
7. 创新点总结（1页）
8. 不足与展望（1页）
9. 致谢

**PPT原则：** 图多字少、一页一个主题、关键数据加粗、不要贴代码。

---

## 九、开放性问题（4题）

### Q52: 你觉得AI在未来教育中会取代老师吗？

**答：** 不会取代，而是辅助。AI擅长的是"重复性劳动"——听学生背诗、批改选择题、统计学情数据，这些事AI做得比人快。但老师真正的价值在于：①情感连接——学生需要的是鼓励和陪伴；②因材施教——"这个孩子今天状态不好，先不逼他"这种判断AI做不到；③言传身教——老师的人格魅力、价值观影响无法被AI替代。

我的系统定位就是"帮老师减负"，让老师把精力花在AI做不了的事情上。

---

### Q53: 做这个项目最大的收获是什么？

**答：** （个性化回答，参考方向：）
1. 技术层面：完整的全栈项目经验——从数据库设计到后端到小程序到AI集成
2. 工程层面：踩了很多坑——ASR v2调试、classpath编译问题、前后端联调——对"调试能力"有了质的提升
3. 产品层面：学会了从用户视角思考——为什么选动画IP角色、为什么精灵球要可拖动、为什么零记录学生不能被漏掉

---

### Q54: 这个项目最让你自豪的是什么？

**答：** AI名师团的设计。它不是一个冷冰冰的"AI老师"，而是6个有性格、有温度、会开玩笑的动画角色。当我第一次听到猴哥的TTS音色说出"俺老孙来也"的时候，我觉得这个东西真的能让孩子喜欢上读古诗。

技术上它也不简单——同一个LLM要通过不同的System Prompt变成6个完全不同的人，而且在语音助手、诗词创作、飞花令三种场景下还要保持人格一致。这套Prompt工程花了很多心思。

---

### Q55: 还有什么想补充的吗？

**答：** （准备一段收尾话术）
"我想强调一点——这个系统不是炫技。每一个AI能力的使用都有明确的教学场景支撑：ASR不是为了做语音识别而做，是为了让不会打字的孩子也能用；LLM不是为了聊天而聊天，是为了给学生一个能问'为什么'的老师；TTS不是为了发声而发声，是为了让AI名师真的有温度。技术服务于教育，这是我一直坚持的原则。"
